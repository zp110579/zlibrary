<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zee.libs.example">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity android:name="com.zee.libs.example.ui.activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activitys.InputActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.activitys.EventBusActivity" />
        <activity android:name=".ui.activitys.LifecycleActivity" />
        <activity android:name=".ui.activitys.SeekBarActivity" />
        <activity android:name=".ui.activitys.BubbleSeekBarActivity" />
        <activity android:name=".ui.activitys.WebViewActivity" />
        <activity android:name=".ui.activitys.TwoTextViewActivity" />
    </application>

</manifest>